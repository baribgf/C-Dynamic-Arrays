{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build / Run",
            "type": "shell",
            "command": "gcc ${workspaceFolder}/src/*.c ${cwd}/*.c  -o bin/${fileBasenameNoExtension} && bin/*",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}